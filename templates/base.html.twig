
<!doctype html>
<html lang="en" data-bs-theme="auto">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>La Boutique Française - 100% Made in France</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQnSeE5e3G1p3y77W4ySi64DzQe6p6zKzm82snfYc4Lr8eXKXpIzKX+I7" crossorigin="anonymous">
    <meta name="description" content="La boutique spécialisée dans la fabrication Made In France">

    <link rel="stylesheet" href="{{asset('assets/css/style.css')}}">

    <link href="{{asset('/assets/css/bootstrap.min.css')}}" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="{{asset('/assets/css/carousel.css')}}" rel="stylesheet">
    <link href="{{asset('/assets/css/custom.css')}} " rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
</head>
<body>
<header data-bs-theme="dark">
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">La Boutique Française</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav me-auto mb-2 mb-md-0">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_home') }}">Home</a>
                    </li>
             
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_category_index') }}">Categories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_product_index') }}">Products</a>
                </li>
                   {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_index') }}">Dashboard</a>
                </li>

            {% endif %}

                </ul>
                <div class="speed-menu">
                    {% if app.user %}
                        <a href="{{ path('app_user_show', {'id': app.user.id}) }}"><i data-feather="user"></i></a>
                    {% else %}
                        <a href="{{ path('login') }}"><i data-feather="user"></i></a>
                    {% endif %}
                    <a href="{{ path('app_cart_index') }}"><i data-feather="shopping-cart"></i></a>
                    {% if app.user %}
                     <a href="{{ path('app_cart_user') }}" class="btn btn-secondary">Mon Panier</a>
                    {% endif %}

                </div>
            </div>
        </div>
    </nav>
</header>


<main>
  {% block body %}

  {% endblock %}
    <!-- FOOTER -->
    <footer class="container">
        <p>&copy; 2017–2024 La Boutique Française <a href="#">CGV</a> &middot; <a href="#">CGU</a></p>
    </footer>
</main>
<script src="{{asset('/assets/js/bootstrap.bundle.min.js')}}"></script>
<script src="{{asset('/assets/js/global.js')}}"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-whvV6HBVArLmoIzQfXdzczKLYddK1bcNVbtU6tvJWxK6tW9KEJtb6BRsyUZXqr2b" crossorigin="anonymous"></script>
</body>
</html>